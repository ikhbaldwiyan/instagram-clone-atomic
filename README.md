
# Instagram Atomic Design

This is example simple atomic design boilerplate with study case instargam design system using Vite JS + Tailwind CSS

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
